---
title: "Exploring Airbnb data in 3 French ciities"
author: "Margot MARCHAIS--MAURICE"
date: '2022-10-24'
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## INTRODUCTION

# Contexte business : 
- Rappeler le business model d'Airbnb
- Rappeler que la France est un des pays les plus touristiques au monde (ranking, sources, etc)
- Mais toutes les villes ne se valent sûrement pas : on va donc comparer la capitale Paris aux autres grosses villes françaises Bordeaux et Lyon

# Objectif de l'étude : 
- Mieux comprendre les annonces publiées en ligne, le profil des hosts, etc avec une segmentation (est-ce que ce sont des professionnels ? )
- D'un point de vue technique : créer une base de donnée globale à partir de différents fichiers via des UNION et JOIN, faire un peu d'analyse sous R (via EDA et stats descriptives), rendre le tout interactif grâce à une appli Rshiny et faire une segmentation pour la partie algorithmie (ou bien faire de l'analyse textuelle à partir des reviews)



### Loading R packages that will allow the connection to databases

```{r}
library(readr)
library(odbc) #Contains drivers to connect to a database
library(DBI) #Contains functions for interacting with the database
library(RSQL) #Generate and Process 'SQL' Queries in R
library(RSQLite) #Can create an in-memory SQL database
library(tidyverse) #Provides helpful functions and sample data to use in R
```

Set up the connection

```{r}
con <- DBI::dbConnect(drv = odbc::odbc(),
                      Driver = "driver_name",
                      Server = "server_url",
                      Database = "database_name",
                      user = "user", #optional
                      password = "password") #optional
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
